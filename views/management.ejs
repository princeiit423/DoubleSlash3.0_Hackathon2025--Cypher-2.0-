<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Crop Management Form</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
       body {
    font-family: 'Poppins', sans-serif;
    background: linear-gradient(135deg, #d4fc79, #96e6a1);
    min-height: 100vh;
    display: flex;
    justify-content: center;
    padding: 40px 0; /* Add padding to ensure scrolling */
    overflow-y: auto; /* Enables vertical scrolling */
}

form {
    max-height: 90vh; /* Prevent form from growing too large */
    overflow-y: auto; /* Ensure form scrolls internally if too long */
}
       .glass {
            background: rgba(255, 255, 255, 0.2);
            border-radius: 20px;
            backdrop-filter: blur(10px);
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
            border: 1px solid rgba(255, 255, 255, 0.3);
        }
        input, select, textarea {
            transition: all 0.3s ease-in-out;
        }
        input:focus, select:focus, textarea:focus {
            transform: scale(1.05);
            box-shadow: 0 0 10px rgba(34, 197, 94, 0.5);
        }
    </style>
</head>
<body>

    <form action="/management" method="POST" enctype="multipart/form-data" class="glass p-10 w-full max-w-5xl">
        <h2 class="text-3xl font-bold text-center text-green-800 mb-8">🌱 Crop Management Portal</h2>
    
        <div class="grid grid-cols-2 gap-8">
            <!-- Left Column: Farmer & Crop Details -->
            <div>
                <h3 class="text-xl font-semibold text-green-700 mb-3">👨‍🌾 Farmer Details</h3>
                <div class="mb-4 flex items-center space-x-4">
                    <label class="w-28 text-green-800">Name</label>
                    <input type="text" name="farmer[name]" class="flex-1 px-4 py-2 border rounded-lg bg-white/70" required>
                </div>
                <div class="mb-4 flex items-center space-x-4">
                    <label class="w-28 text-green-800">Contact</label>
                    <input type="text" name="farmer[contact]" class="flex-1 px-4 py-2 border rounded-lg bg-white/70" required>
                </div>
                <div class="mb-4 flex items-center space-x-4">
                    <label class="w-28 text-green-800">City</label>
                    <input type="text" name="farmer[location][city]" class="flex-1 px-4 py-2 border rounded-lg bg-white/70">
                </div>
                <div class="mb-4 flex items-center space-x-4">
                    <label class="w-28 text-green-800">State</label>
                    <input type="text" name="farmer[location][state]" class="flex-1 px-4 py-2 border rounded-lg bg-white/70">
                </div>
                <div class="mb-4 flex items-center space-x-4">
                    <label class="w-28 text-green-800">Country</label>
                    <input type="text" name="farmer[location][country]" class="flex-1 px-4 py-2 border rounded-lg bg-white/70">
                </div>
    
                <h3 class="text-xl font-semibold text-green-700 mt-6 mb-3">🌿 Crop Details</h3>
                <div class="mb-4 flex items-center space-x-4">
                    <label class="w-28 text-green-800">Crop Name</label>
                    <input type="text" name="cropDetails[name]" class="flex-1 px-4 py-2 border rounded-lg bg-white/70">
                </div>
                <div class="mb-4 flex items-center space-x-4">
                    <label class="w-28 text-green-800">Variety</label>
                    <input type="text" name="cropDetails[variety]" class="flex-1 px-4 py-2 border rounded-lg bg-white/70">
                </div>
                <div class="mb-4 flex items-center space-x-4">
                    <label class="w-28 text-green-800">Sowing Date</label>
                    <input type="date" name="cropDetails[sowingDate]" class="flex-1 px-4 py-2 border rounded-lg bg-white/70">
                </div>
                <div class="mb-4 flex items-center space-x-4">
                    <label class="w-28 text-green-800">Harvesting Date</label>
                    <input type="date" name="cropDetails[harvestingDate]" class="flex-1 px-4 py-2 border rounded-lg bg-white/70">
                </div>
            </div>
    
            <!-- Right Column: Soil, Fertilizer & Plant Health -->
            <div>
                <h3 class="text-xl font-semibold text-green-700 mb-3">🌍 Soil & Weather</h3>
                <div class="mb-4 flex items-center space-x-4">
                    <label class="w-32 text-green-800">Soil Type</label>
                    <input type="text" name="soilAndWeather[soilType]" class="flex-1 px-4 py-2 border rounded-lg bg-white/70">
                </div>
                <div class="mb-4 flex items-center space-x-4">
                    <label class="w-32 text-green-800">pH Level</label>
                    <input type="number" step="0.1" name="soilAndWeather[pHLevel]" class="flex-1 px-4 py-2 border rounded-lg bg-white/70">
                </div>
                <div class="mb-4 flex items-center space-x-4">
                    <label class="w-32 text-green-800">Moisture Level</label>
                    <input type="number" step="0.1" name="soilAndWeather[moistureLevel]" class="flex-1 px-4 py-2 border rounded-lg bg-white/70">
                </div>
                <div class="mb-4 flex items-center space-x-4">
                    <label class="w-32 text-green-800">Temperature (°C)</label>
                    <input type="number" step="0.1" name="soilAndWeather[temperature]" class="flex-1 px-4 py-2 border rounded-lg bg-white/70">
                </div>
                <div class="mb-4 flex items-center space-x-4">
                    <label class="w-32 text-green-800">Rainfall (mm)</label>
                    <input type="number" step="0.1" name="soilAndWeather[rainfall]" class="flex-1 px-4 py-2 border rounded-lg bg-white/70">
                </div>
    
                <h3 class="text-xl font-semibold text-green-700 mt-6 mb-3">🧪 Fertilizer & Pesticide</h3>
                <div class="mb-4 flex items-center space-x-4">
                    <label class="w-32 text-green-800">Fertilizer</label>
                    <input type="text" name="fertilizerUsage[fertilizerType]" class="flex-1 px-4 py-2 border rounded-lg bg-white/70">
                </div>
                <div class="mb-4 flex items-center space-x-4">
                    <label class="w-32 text-green-800">Application Date</label>
                    <input type="date" name="fertilizerUsage[applicationDate]" class="flex-1 px-4 py-2 border rounded-lg bg-white/70">
                </div>
                <div class="mb-4 flex items-center space-x-4">
                    <label class="w-32 text-green-800">Pesticide</label>
                    <input type="text" name="pesticideUsage[ pesticideType]" class="flex-1 px-4 py-2 border rounded-lg bg-white/70">
                </div>
                <div class="mb-4 flex items-center space-x-4">
                    <label class="w-32 text-green-800">Application Date</label>
                    <input type="date" name="pesticideUsage[applicationDate]" class="flex-1 px-4 py-2 border rounded-lg bg-white/70">
                </div>
    
                <h3 class="text-xl font-semibold text-green-700 mt-6 mb-3">🍃 Plant Health</h3>
                <div class="mb-4 flex items-center space-x-4">
                    <label class="w-32 text-green-800">Symptoms?</label>
                    <select name="plantHealth[hasDiseaseSymptoms]" class="flex-1 px-4 py-2 border rounded-lg bg-white/70">
                        <option value="false">No</option>
                        <option value="true">Yes</option>
                    </select>
                </div>
                <div class="mb-4 flex items-center space-x-4">
                    <label class="w-32 text-green-800">Description</label>
                    <textarea name="plantHealth[symptomsDescription]" class="flex-1 px-4 py-2 border rounded-lg bg-white/70"></textarea>
                </div>
                
            </div>
        </div>
    
        <div class="text-center mt-8">
            <button type="submit" class="px-6 py-3 bg-green-600 text-white rounded-lg">Submit 🌾</button>
        </div>
    </form>
    
</body>
</html>
